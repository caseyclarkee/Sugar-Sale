Drop this `public/` folder into the root of your Vite/React project.
- Vite will serve `public/index.html` as-is (no bundling required).
- Netlify forms are prewired; submissions will appear under “interest”.
- Age gate matches the teaser (localStorage `ageGatePass`, 1-hour expiry).
